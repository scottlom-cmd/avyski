// Phase 2: risk model - direct trigger (slope angle x aspect x danger
// rating), remote triggering via grid adjacency to connected steeper
// terrain, terrain-trap consequence multiplier (using the `trap` layer
// terrain.js already exposes per cell), ridge/pocket transit safety.
